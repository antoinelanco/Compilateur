
main(integer x) (
  var []integer tab;
  var integer i;


  tab := [x]integer;

  for (i := 0; i < 100; i := i+1 ) (
    tab[i] := i + 49;
  );

  for (i := 0; i < 100; i := i+1 ) (
    print(tab[i]);
  );
)
